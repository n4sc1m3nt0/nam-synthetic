# Phase 1 — dataset preparation (parsing a self-curated .nam collection)
# nam_parser uses only the Python standard library; no runtime deps required.

# Phase 2, 3, 4 — ML training (install when you reach those phases)
# torch>=2.2.0
# numpy>=1.26.0
# scikit-learn>=1.4.0
# sentence-transformers>=2.7.0
# matplotlib>=3.8.0
